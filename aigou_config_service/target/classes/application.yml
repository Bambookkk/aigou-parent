server:
  port: 1299
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    prefer-ip-address: true #悬停时右下角显示ip
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Bambookkk/aigou-config.git  #配置文件地址
          username: Bambookkk
          password: Kbs170917Git